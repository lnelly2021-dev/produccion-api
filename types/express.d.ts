/**
 * Aumentos de tipo para Express.
 * Las extensiones de `Request` están en los middlewares que las introducen
 * (ej. `auth.middleware.ts` agrega `req.user`).
 *
 * Este archivo está reservado para tipos globales del namespace Express
 * que no pertenezcan a un middleware específico.
 */

export {};
